diff --git a/docs/guides/README.md b/docs/guides/README.md
index 46ca211..7b37a2d 100644
--- a/docs/guides/README.md
+++ b/docs/guides/README.md
@@ -9,7 +9,7 @@ Perplexity／開發者看的工程規格與流程規則。這個目錄放**給
 | 檔案 | 說明 | 對應資料源 |
 |---|---|---|
 | `fleet-risk-register.html` | 機隊風險登記冊——33 種已知錯誤情況，依四個風險等級（v2）白話說明、處理步驟與給 AI 的追查指令，並標示各策略實際做到了沒有；可依等級／策略／未做完篩選。 | `src/trade_alerts/catalog/fleet-error-catalog-v2.json`＋`fleet-rollout-registry.json` |
-| `fleet-rollout-register.html` | 機隊一致性登記冊——每項機隊功能與每條錯誤規則在四支策略的已做／未做（含預定哪一關）／不適用（含理由）；分「未完成／已完成」兩組，最上方目錄可點擊跳到該項，已完成項目預設摺疊，右下角有回到最上方按鈕。 | `src/trade_alerts/catalog/fleet-rollout-registry.json` |
+| `fleet-rollout-register.html` | 機隊一致性登記冊——每項機隊功能與每條錯誤規則在四支策略的已做／未做（含預定哪一關）／不適用（含理由）；分「未完成／已完成」兩組，最上方目錄可點擊跳到該項，已完成項目預設摺疊，右下角有回到最上方按鈕。JSON 的 `recent_changes` 可以標出這次更新剛變成已做／不適用的格子，頁面用「🆕 最新完成」黃色標示並列在最上方的「最近變動」欄，同時兼作「這次更新有沒有真的生效」的視覺確認。 | `src/trade_alerts/catalog/fleet-rollout-registry.json` |
 
 ## 慣例
 
diff --git a/docs/guides/fleet-risk-register.html b/docs/guides/fleet-risk-register.html
index 42a0d32..2c74fce 100644
--- a/docs/guides/fleet-risk-register.html
+++ b/docs/guides/fleet-risk-register.html
@@ -12,6 +12,7 @@
     --done:#357357; --done-bg:#DEEAE3;
     --pending:#8C6317; --pending-bg:#F1E6C9;
     --na:#6B7566; --na-bg:#E7EAE0;
+    --recent:#A6800A; --recent-bg:#FFF3B0;
   }
   @media (prefers-color-scheme: dark){
     :root:not([data-theme="light"]){
@@ -21,6 +22,7 @@
       --R3:#E58A7C; --R3-bg:#3A241F; --R2:#D9B364; --R2-bg:#382D18;
       --R1:#7FC6A2; --R1-bg:#1D3227; --R0:#A6AD97; --R0-bg:#292B21;
       --done:#7FC6A2; --done-bg:#1D3227; --pending:#D9B364; --pending-bg:#382D18; --na:#A6AD97; --na-bg:#292B21;
+      --recent:#FFE066; --recent-bg:#4A3B05;
     }
   }
   :root[data-theme="dark"]{
@@ -30,6 +32,7 @@
     --R3:#E58A7C; --R3-bg:#3A241F; --R2:#D9B364; --R2-bg:#382D18;
     --R1:#7FC6A2; --R1-bg:#1D3227; --R0:#A6AD97; --R0-bg:#292B21;
     --done:#7FC6A2; --done-bg:#1D3227; --pending:#D9B364; --pending-bg:#382D18; --na:#A6AD97; --na-bg:#292B21;
+    --recent:#FFE066; --recent-bg:#4A3B05;
   }
   *{box-sizing:border-box;}
   body{margin:0;background:var(--paper);color:var(--ink);font-family:"Noto Sans TC","IBM Plex Mono",sans-serif;padding:40px 20px 64px;}
diff --git a/docs/guides/fleet-rollout-register.html b/docs/guides/fleet-rollout-register.html
index fa34529..961be72 100644
--- a/docs/guides/fleet-rollout-register.html
+++ b/docs/guides/fleet-rollout-register.html
@@ -12,6 +12,7 @@
     --done:#357357; --done-bg:#DEEAE3;
     --pending:#8C6317; --pending-bg:#F1E6C9;
     --na:#6B7566; --na-bg:#E7EAE0;
+    --recent:#A6800A; --recent-bg:#FFF3B0;
   }
   @media (prefers-color-scheme: dark){
     :root:not([data-theme="light"]){
@@ -21,6 +22,7 @@
       --R3:#E58A7C; --R3-bg:#3A241F; --R2:#D9B364; --R2-bg:#382D18;
       --R1:#7FC6A2; --R1-bg:#1D3227; --R0:#A6AD97; --R0-bg:#292B21;
       --done:#7FC6A2; --done-bg:#1D3227; --pending:#D9B364; --pending-bg:#382D18; --na:#A6AD97; --na-bg:#292B21;
+      --recent:#FFE066; --recent-bg:#4A3B05;
     }
   }
   :root[data-theme="dark"]{
@@ -30,6 +32,7 @@
     --R3:#E58A7C; --R3-bg:#3A241F; --R2:#D9B364; --R2-bg:#382D18;
     --R1:#7FC6A2; --R1-bg:#1D3227; --R0:#A6AD97; --R0-bg:#292B21;
     --done:#7FC6A2; --done-bg:#1D3227; --pending:#D9B364; --pending-bg:#382D18; --na:#A6AD97; --na-bg:#292B21;
+    --recent:#FFE066; --recent-bg:#4A3B05;
   }
   *{box-sizing:border-box;}
   body{margin:0;background:var(--paper);color:var(--ink);font-family:"Noto Sans TC","IBM Plex Mono",sans-serif;padding:40px 20px 64px;}
@@ -106,8 +109,17 @@
   .dot{display:inline-block;width:9px;height:9px;border-radius:50%;background:var(--dc);border:1px solid var(--dc);}
   .dot-done{--dc:var(--done);} .dot-pending{--dc:var(--pending);} .dot-na{--dc:var(--na);opacity:.55;}
   .dot-none{--dc:var(--line);background:transparent;}
+  .dot-recent{--dc:var(--recent);box-shadow:0 0 0 2px var(--recent-bg);}
   .dot-legend{display:flex;flex-wrap:wrap;gap:4px 14px;font-size:12px;color:var(--ink-faint);margin:0 0 4px;}
   .dot-legend span{display:inline-flex;align-items:center;gap:5px;}
+  .new-badge{display:inline-block;font-size:10px;font-weight:800;letter-spacing:.03em;color:var(--recent);background:var(--recent-bg);border-radius:999px;padding:1px 7px;margin-inline-start:4px;white-space:nowrap;vertical-align:middle;}
+  .recent-box{border:1px solid var(--recent);border-radius:3px;background:var(--recent-bg);padding:12px 14px;margin-bottom:14px;}
+  .recent-box h3{margin:0 0 8px;font-size:13px;color:var(--recent);}
+  .recent-box ul{list-style:none;margin:0;padding:0;display:flex;flex-direction:column;gap:8px;}
+  .recent-box li a{color:inherit;text-decoration:none;font-weight:700;font-size:13.5px;}
+  .recent-box li a:hover,.recent-box li a:focus-visible{text-decoration:underline;}
+  .recent-box .proj{font-weight:600;font-size:11.5px;color:var(--ink-soft);margin-inline-start:6px;}
+  .recent-box .note{display:block;font-weight:400;font-size:12.5px;color:var(--ink-soft);margin-top:2px;}
   .group-head{display:flex;align-items:baseline;gap:10px;margin:40px 0 4px;padding-bottom:8px;border-block-end:2px solid var(--c);}
   .group-head h2{margin:0;color:var(--c);}
   .group-head .n{font-family:"IBM Plex Mono","Noto Sans TC",monospace;font-size:13px;color:var(--ink-faint);}
@@ -160,7 +172,12 @@
     <span>每項右邊的四個點依序是 趨勢策略、動能策略、加密策略、競賽策略：</span>
     <span><i class="dot dot-done"></i>已做</span><span><i class="dot dot-pending"></i>未做</span>
     <span><i class="dot dot-na"></i>不適用</span><span><i class="dot dot-none"></i>跟這支無關</span>
+    <span><i class="dot dot-recent"></i>🆕 最新完成——這次更新才變成已做／不適用</span>
   </p>
+  <div class="recent-box" id="recentBox" hidden>
+    <h3>🆕 最近變動——確認這次更新真的生效了</h3>
+    <ul id="recentList"></ul>
+  </div>
   <nav class="toc" id="toc" aria-label="目錄"></nav>
 
   <div class="group-head c-pending" id="group-pending"><h2>未完成</h2><span class="n">38 項</span></div>
@@ -194,6 +211,8 @@
     "mycrypto": "na",
     "seykota": "done"
    },
+   "has_recent": false,
+   "id": "latch.unified_model",
    "kind": "cap",
    "rows": [
     {
@@ -253,6 +272,8 @@
     "mycrypto": "na",
     "seykota": "done"
    },
+   "has_recent": false,
+   "id": "latch.resume_tool",
    "kind": "cap",
    "rows": [
     {
@@ -312,6 +333,8 @@
     "mycrypto": "na",
     "seykota": "pending"
    },
+   "has_recent": false,
+   "id": "latch.restart_only_clear",
    "kind": "cap",
    "rows": [
     {
@@ -371,6 +394,8 @@
     "mycrypto": "pending",
     "seykota": "done"
    },
+   "has_recent": false,
+   "id": "latch.consecutive_failure_counter",
    "kind": "cap",
    "rows": [
     {
@@ -430,6 +455,8 @@
     "mycrypto": "done",
     "seykota": "done"
    },
+   "has_recent": false,
+   "id": "runtime.status_snapshot",
    "kind": "cap",
    "rows": [
     {
@@ -489,6 +516,8 @@
     "mycrypto": "done",
     "seykota": "done"
    },
+   "has_recent": false,
+   "id": "reconcile.ledger_status",
    "kind": "cap",
    "rows": [
     {
@@ -548,6 +577,8 @@
     "mycrypto": "done",
     "seykota": "done"
    },
+   "has_recent": false,
+   "id": "reconcile.google_status",
    "kind": "cap",
    "rows": [
     {
@@ -607,6 +638,8 @@
     "mycrypto": "pending",
     "seykota": "pending"
    },
+   "has_recent": false,
+   "id": "repair_bot.verified_close",
    "kind": "cap",
    "rows": [
     {
@@ -666,6 +699,8 @@
     "mycrypto": "pending",
     "seykota": "pending"
    },
+   "has_recent": false,
+   "id": "repair_bot.pause_brake",
    "kind": "cap",
    "rows": [
     {
@@ -721,10 +756,12 @@
    "complete": false,
    "dots": {
     "btc-competition": "pending",
-    "momentum": "done",
+    "momentum": "recent",
     "mycrypto": "pending",
     "seykota": "pending"
    },
+   "has_recent": true,
+   "id": "ops_export.writer",
    "kind": "cap",
    "rows": [
     {
@@ -784,6 +821,8 @@
     "mycrypto": "pending",
     "seykota": "pending"
    },
+   "has_recent": false,
+   "id": "ops_notify.export_relay_enabled",
    "kind": "cap",
    "rows": [
     {
@@ -843,6 +882,8 @@
     "mycrypto": "pending",
     "seykota": "pending"
    },
+   "has_recent": false,
+   "id": "ops_notify.units_axis",
    "kind": "cap",
    "rows": [
     {
@@ -902,6 +943,8 @@
     "mycrypto": "done",
     "seykota": "pending"
    },
+   "has_recent": false,
+   "id": "reconcile.funding_fee_sync",
    "kind": "cap",
    "rows": [
     {
@@ -961,6 +1004,8 @@
     "mycrypto": "none",
     "seykota": "pending"
    },
+   "has_recent": false,
+   "id": "SEY.PROTECTION_PLACEMENT_FAILED_FLATTENED",
    "kind": "rule",
    "rows": [
     {
@@ -993,6 +1038,8 @@
     "mycrypto": "none",
     "seykota": "pending"
    },
+   "has_recent": false,
+   "id": "SEY.PROTECTION_PLACEMENT_FAILED_EXPOSED",
    "kind": "rule",
    "rows": [
     {
@@ -1025,6 +1072,8 @@
     "mycrypto": "none",
     "seykota": "pending"
    },
+   "has_recent": false,
+   "id": "SEY.PROTECTION_REPLACE_FAILED",
    "kind": "rule",
    "rows": [
     {
@@ -1057,6 +1106,8 @@
     "mycrypto": "none",
     "seykota": "pending"
    },
+   "has_recent": false,
+   "id": "SEY.PROTECTION_ORPHAN_CANCEL_FAILED",
    "kind": "rule",
    "rows": [
     {
@@ -1089,6 +1140,8 @@
     "mycrypto": "none",
     "seykota": "pending"
    },
+   "has_recent": false,
+   "id": "SEY.PROTECTION_CLOSE_CANCEL_FAILED",
    "kind": "rule",
    "rows": [
     {
@@ -1121,6 +1174,8 @@
     "mycrypto": "none",
     "seykota": "pending"
    },
+   "has_recent": false,
+   "id": "SEY.PROTECTION_UNVERIFIED",
    "kind": "rule",
    "rows": [
     {
@@ -1153,6 +1208,8 @@
     "mycrypto": "none",
     "seykota": "pending"
    },
+   "has_recent": false,
+   "id": "SEY.POSITION_AMBIGUOUS",
    "kind": "rule",
    "rows": [
     {
@@ -1185,6 +1242,8 @@
     "mycrypto": "none",
     "seykota": "pending"
    },
+   "has_recent": false,
+   "id": "SEY.RECONCILE_FAILED",
    "kind": "rule",
    "rows": [
     {
@@ -1217,6 +1276,8 @@
     "mycrypto": "none",
     "seykota": "pending"
    },
+   "has_recent": false,
+   "id": "SEY.EXCHANGE_TARGET_UNSAFE",
    "kind": "rule",
    "rows": [
     {
@@ -1249,6 +1310,8 @@
     "mycrypto": "none",
     "seykota": "pending"
    },
+   "has_recent": false,
+   "id": "SEY.FIXED_IDENTIFIER_CONTRACT_UNAVAILABLE",
    "kind": "rule",
    "rows": [
     {
@@ -1281,6 +1344,8 @@
     "mycrypto": "none",
     "seykota": "pending"
    },
+   "has_recent": false,
+   "id": "SEY.RUNTIME_CYCLE_FAILED",
    "kind": "rule",
    "rows": [
     {
@@ -1313,6 +1378,8 @@
     "mycrypto": "none",
     "seykota": "pending"
    },
+   "has_recent": false,
+   "id": "SEY.CLOSE_FILL_PENDING",
    "kind": "rule",
    "rows": [
     {
@@ -1345,6 +1412,8 @@
     "mycrypto": "none",
     "seykota": "pending"
    },
+   "has_recent": false,
+   "id": "SEY.TRADE_EXIT",
    "kind": "rule",
    "rows": [
     {
@@ -1377,6 +1446,8 @@
     "mycrypto": "none",
     "seykota": "pending"
    },
+   "has_recent": false,
+   "id": "SEY.ENTRY_SKIPPED_MIN_CAPITAL",
    "kind": "rule",
    "rows": [
     {
@@ -1409,6 +1480,8 @@
     "mycrypto": "none",
     "seykota": "none"
    },
+   "has_recent": false,
+   "id": "MOM.PROTECTION_UNVERIFIED",
    "kind": "rule",
    "rows": [
     {
@@ -1441,6 +1514,8 @@
     "mycrypto": "none",
     "seykota": "none"
    },
+   "has_recent": false,
+   "id": "MOM.STATE_REPAIRED_SAFE_HALT",
    "kind": "rule",
    "rows": [
     {
@@ -1473,6 +1548,8 @@
     "mycrypto": "none",
     "seykota": "none"
    },
+   "has_recent": false,
+   "id": "MOM.RUNTIME_CYCLE_FAILED",
    "kind": "rule",
    "rows": [
     {
@@ -1505,6 +1582,8 @@
     "mycrypto": "none",
     "seykota": "none"
    },
+   "has_recent": false,
+   "id": "MOM.VERIFIED_CLOSE_AUTO_REPAIRED",
    "kind": "rule",
    "rows": [
     {
@@ -1537,6 +1616,8 @@
     "mycrypto": "none",
     "seykota": "none"
    },
+   "has_recent": false,
+   "id": "MOM.VERIFIED_CLOSE_PROPOSED",
    "kind": "rule",
    "rows": [
     {
@@ -1569,6 +1650,8 @@
     "mycrypto": "none",
     "seykota": "none"
    },
+   "has_recent": false,
+   "id": "MOM.VERIFIED_CLOSE_REPAIR_BLOCKED",
    "kind": "rule",
    "rows": [
     {
@@ -1601,6 +1684,8 @@
     "mycrypto": "none",
     "seykota": "none"
    },
+   "has_recent": false,
+   "id": "BTC.BOOK_CORRUPT_NEGATIVE_BALANCE",
    "kind": "rule",
    "rows": [
     {
@@ -1633,6 +1718,8 @@
     "mycrypto": "none",
     "seykota": "none"
    },
+   "has_recent": false,
+   "id": "BTC.EXECUTION_BLOCKED_ZERO_FILLS",
    "kind": "rule",
    "rows": [
     {
@@ -1665,6 +1752,8 @@
     "mycrypto": "none",
     "seykota": "none"
    },
+   "has_recent": false,
+   "id": "BTC.REBALANCE_PENDING",
    "kind": "rule",
    "rows": [
     {
@@ -1697,6 +1786,8 @@
     "mycrypto": "none",
     "seykota": "none"
    },
+   "has_recent": false,
+   "id": "BTC.RUNTIME_CYCLE_FAILED",
    "kind": "rule",
    "rows": [
     {
@@ -1729,6 +1820,8 @@
     "mycrypto": "done",
     "seykota": "none"
    },
+   "has_recent": false,
+   "id": "MYC.RECONCILE_DELTA_EXCEEDED",
    "kind": "rule",
    "rows": [
     {
@@ -1761,6 +1854,8 @@
     "mycrypto": "pending",
     "seykota": "none"
    },
+   "has_recent": false,
+   "id": "MYC.RUNTIME_CYCLE_FAILED",
    "kind": "rule",
    "rows": [
     {
@@ -1793,6 +1888,8 @@
     "mycrypto": "pending",
     "seykota": "none"
    },
+   "has_recent": false,
+   "id": "MYC.PROTECTION_PLACEMENT_REJECTED",
    "kind": "rule",
    "rows": [
     {
@@ -1825,6 +1922,8 @@
     "mycrypto": "pending",
     "seykota": "none"
    },
+   "has_recent": false,
+   "id": "MYC.PROTECTION_PLACEMENT_NO_POSITION",
    "kind": "rule",
    "rows": [
     {
@@ -1857,6 +1956,8 @@
     "mycrypto": "pending",
     "seykota": "none"
    },
+   "has_recent": false,
+   "id": "MYC.PROTECTION_PLACEMENT_ERROR",
    "kind": "rule",
    "rows": [
     {
@@ -1889,6 +1990,8 @@
     "mycrypto": "pending",
     "seykota": "none"
    },
+   "has_recent": false,
+   "id": "MYC.PROTECTION_REPLACE_FAILED",
    "kind": "rule",
    "rows": [
     {
@@ -1921,6 +2024,8 @@
     "mycrypto": "pending",
     "seykota": "none"
    },
+   "has_recent": false,
+   "id": "MYC.PROTECTION_ORPHAN_CANCEL_FAILED",
    "kind": "rule",
    "rows": [
     {
@@ -1953,6 +2058,8 @@
     "mycrypto": "done",
     "seykota": "done"
    },
+   "has_recent": false,
+   "id": "FLEET.LEDGER_DIVERGED_UNIT_EXIT",
    "kind": "rule",
    "rows": [
     {
@@ -2036,6 +2143,8 @@
     "mycrypto": "done",
     "seykota": "done"
    },
+   "has_recent": false,
+   "id": "FLEET.GOOGLE_DIVERGED_UNIT_EXIT",
    "kind": "rule",
    "rows": [
     {
@@ -2156,6 +2265,14 @@
   "mycrypto": "加密策略",
   "seykota": "趨勢策略"
  },
+ "recent_summary": [
+  {
+   "anchor": "cap-ops_export.writer",
+   "note": "今天 F3 資金費修復整支重新部署，順帶把這支也帶上了真倉；scripts/repair_bot.py:278 refresh_ops_export 每輪都在改寫 audit/ops_export.json（目前內容是空的）。",
+   "project": "動能策略",
+   "title": "把要通知的事寫成匯出檔"
+  }
+ ],
  "totals": {
   "btc-competition": {
    "done": 10,
@@ -2196,13 +2313,14 @@ function chipGroup(host, values, onPick){
   });
 }
 
-var DOT_LABEL={"done":"已做","pending":"未做","na":"不適用","none":"不相關"};
+var DOT_LABEL={"done":"已做","pending":"未做","na":"不適用","none":"不相關","recent":"剛完成"};
 var KINDS=[["cap","機隊功能"],["rule","錯誤處理規則"]];
 function dots(item){
   var s=el("span","dots"); s.setAttribute("aria-hidden","true");
   DATA.project_order.forEach(function(p){ var d=el("span","dot dot-"+item.dots[p]); d.title=DATA.projects[p]+"："+DOT_LABEL[item.dots[p]]; s.appendChild(d); });
   return s;
 }
+function newBadge(){ var b=el("span","new-badge","🆕 最新"); b.title="這次更新剛變成已做／不適用"; return b; }
 function table(item){
   var g=el("div","sl");
   item.rows.forEach(function(r){
@@ -2224,13 +2342,16 @@ function table(item){
 function pendingCard(item){
   var c=el("div","card item"); c.id=item.anchor;
   var head=el("div","item-head"), h=el("div","h");
-  h.appendChild(el("h3",null,item.title)); h.appendChild(el("p",item.kind==="cap"?"desc":"code",item.sub));
+  h.appendChild(el("h3",null,item.title)); if(item.has_recent) h.appendChild(newBadge());
+  h.appendChild(el("p",item.kind==="cap"?"desc":"code",item.sub));
   head.appendChild(h); head.appendChild(dots(item)); c.appendChild(head);
   c.appendChild(table(item)); return c;
 }
 function doneCard(item){
   var d=el("details","card item done-card"); d.id=item.anchor;
-  var s=el("summary"); s.appendChild(el("span","ok","✓")); s.appendChild(el("span","t",item.title)); s.appendChild(dots(item));
+  var s=el("summary"); s.appendChild(el("span","ok","✓")); s.appendChild(el("span","t",item.title));
+  if(item.has_recent) s.appendChild(newBadge());
+  s.appendChild(dots(item));
   d.appendChild(s);
   var body=el("div","body"); body.appendChild(el("p",item.kind==="cap"?"desc":"code",item.sub)); body.appendChild(table(item));
   d.appendChild(body); return d;
@@ -2252,7 +2373,8 @@ function tocColumn(group, title){
     var ul=el("ul");
     list.forEach(function(i){
       var li=el("li"), a=el("a"); a.href="#"+i.anchor;
-      a.appendChild(el("span","t",i.title)); a.appendChild(dots(i));
+      a.appendChild(el("span","t",i.title)); if(i.has_recent) a.appendChild(newBadge());
+      a.appendChild(dots(i));
       a.addEventListener("click",function(ev){ ev.preventDefault(); go(i.anchor); });
       li.appendChild(a); ul.appendChild(li);
     });
@@ -2267,6 +2389,18 @@ function fillGroup(host, group){
     list.forEach(function(i){ host.appendChild(group==="done"?doneCard(i):pendingCard(i)); });
   });
 }
+var recentBox=document.getElementById("recentBox"), recentList=document.getElementById("recentList");
+if(DATA.recent_summary && DATA.recent_summary.length){
+  DATA.recent_summary.forEach(function(r){
+    var li=el("li"), a=el("a"); a.href="#"+r.anchor;
+    a.appendChild(document.createTextNode(r.title));
+    a.appendChild(el("span","proj",r.project));
+    a.appendChild(el("span","note",r.note));
+    a.addEventListener("click",function(ev){ ev.preventDefault(); go(r.anchor); });
+    li.appendChild(a); recentList.appendChild(li);
+  });
+  recentBox.hidden=false;
+}
 var toc=document.getElementById("toc");
 toc.appendChild(tocColumn("pending","未完成")); toc.appendChild(tocColumn("done","已完成"));
 fillGroup(document.getElementById("pending"),"pending");
diff --git a/schemas/fleet-rollout-registry-v1.schema.json b/schemas/fleet-rollout-registry-v1.schema.json
index f89e516..106657d 100644
--- a/schemas/fleet-rollout-registry-v1.schema.json
+++ b/schemas/fleet-rollout-registry-v1.schema.json
@@ -4,73 +4,210 @@
   "title": "Fleet rollout registry v1",
   "description": "Which fleet capability each of the four strategies actually has, and whether each catalog condition's behaviour and event-log wiring exist in the strategy that owns it. Anything not done is pending (with the phase it lands in and why) or n/a (with why). Cross-field rules (all four strategies listed, pending phases registered, one row per catalog code with the right owners) are enforced by trade_alerts.rollout_registry.registry_problems.",
   "type": "object",
-  "required": ["registry_version", "catalog_version", "updated_at", "projects", "phases", "sources", "capabilities", "catalog"],
+  "required": [
+    "registry_version",
+    "catalog_version",
+    "updated_at",
+    "projects",
+    "phases",
+    "sources",
+    "capabilities",
+    "catalog"
+  ],
   "additionalProperties": false,
   "definitions": {
     "status": {
       "type": "object",
-      "required": ["state"],
+      "required": [
+        "state"
+      ],
       "additionalProperties": false,
       "properties": {
-        "state": {"enum": ["done", "pending", "n/a"]},
-        "evidence": {"type": "string", "minLength": 1, "description": "done: where it lives on the sources commit (file:line or tool name)."},
-        "phase": {"type": "string", "minLength": 1, "description": "pending: a key of phases."},
-        "reason": {"type": "string", "minLength": 1, "description": "pending / n/a: plain-language current state or why it does not apply."}
+        "state": {
+          "enum": [
+            "done",
+            "pending",
+            "n/a"
+          ]
+        },
+        "evidence": {
+          "type": "string",
+          "minLength": 1,
+          "description": "done: where it lives on the sources commit (file:line or tool name)."
+        },
+        "phase": {
+          "type": "string",
+          "minLength": 1,
+          "description": "pending: a key of phases."
+        },
+        "reason": {
+          "type": "string",
+          "minLength": 1,
+          "description": "pending / n/a: plain-language current state or why it does not apply."
+        }
       },
       "allOf": [
-        {"if": {"properties": {"state": {"const": "done"}}}, "then": {"required": ["evidence"]}},
-        {"if": {"properties": {"state": {"const": "pending"}}}, "then": {"required": ["phase", "reason"]}},
-        {"if": {"properties": {"state": {"const": "n/a"}}}, "then": {"required": ["reason"]}}
+        {
+          "if": {
+            "properties": {
+              "state": {
+                "const": "done"
+              }
+            }
+          },
+          "then": {
+            "required": [
+              "evidence"
+            ]
+          }
+        },
+        {
+          "if": {
+            "properties": {
+              "state": {
+                "const": "pending"
+              }
+            }
+          },
+          "then": {
+            "required": [
+              "phase",
+              "reason"
+            ]
+          }
+        },
+        {
+          "if": {
+            "properties": {
+              "state": {
+                "const": "n/a"
+              }
+            }
+          },
+          "then": {
+            "required": [
+              "reason"
+            ]
+          }
+        }
       ]
     },
     "per_project": {
       "type": "object",
       "minProperties": 1,
-      "propertyNames": {"enum": ["momentum", "seykota", "mycrypto", "btc-competition"]},
-      "additionalProperties": {"$ref": "#/definitions/status"}
+      "propertyNames": {
+        "enum": [
+          "momentum",
+          "seykota",
+          "mycrypto",
+          "btc-competition"
+        ]
+      },
+      "additionalProperties": {
+        "$ref": "#/definitions/status"
+      }
     }
   },
   "properties": {
-    "registry_version": {"const": "fleet-rollout-registry/v1"},
-    "catalog_version": {"type": "string"},
-    "updated_at": {"type": "string", "pattern": "^\\d{4}-\\d{2}-\\d{2}$"},
-    "note": {"type": "string"},
+    "registry_version": {
+      "const": "fleet-rollout-registry/v1"
+    },
+    "catalog_version": {
+      "type": "string"
+    },
+    "updated_at": {
+      "type": "string",
+      "pattern": "^\\d{4}-\\d{2}-\\d{2}$"
+    },
+    "note": {
+      "type": "string"
+    },
     "sources": {
       "description": "What 'done' is measured against: per strategy, the repo and the branch the live host deploys from, and the commit the file:line evidence refers to. Merged to development or running on the dev host is not done.",
       "type": "object",
-      "required": ["momentum", "seykota", "mycrypto", "btc-competition"],
+      "required": [
+        "momentum",
+        "seykota",
+        "mycrypto",
+        "btc-competition"
+      ],
       "additionalProperties": false,
       "patternProperties": {
         "^(momentum|seykota|mycrypto|btc-competition)$": {
           "type": "object",
-          "required": ["repo", "branch", "commit"],
+          "required": [
+            "repo",
+            "branch",
+            "commit"
+          ],
           "additionalProperties": false,
           "properties": {
-            "repo": {"type": "string", "pattern": "^[\\w.-]+/[\\w.-]+$"},
-            "branch": {"const": "operations"},
-            "commit": {"type": "string", "pattern": "^[0-9a-f]{40}$"}
+            "repo": {
+              "type": "string",
+              "pattern": "^[\\w.-]+/[\\w.-]+$"
+            },
+            "branch": {
+              "const": "operations"
+            },
+            "commit": {
+              "type": "string",
+              "pattern": "^[0-9a-f]{40}$"
+            }
           }
         }
       }
     },
     "projects": {
       "type": "object",
-      "required": ["momentum", "seykota", "mycrypto", "btc-competition"],
+      "required": [
+        "momentum",
+        "seykota",
+        "mycrypto",
+        "btc-competition"
+      ],
       "additionalProperties": false,
-      "patternProperties": {".*": {"type": "string", "minLength": 1}}
+      "patternProperties": {
+        ".*": {
+          "type": "string",
+          "minLength": 1
+        }
+      }
+    },
+    "phases": {
+      "type": "object",
+      "minProperties": 1,
+      "additionalProperties": {
+        "type": "string",
+        "minLength": 1
+      }
     },
-    "phases": {"type": "object", "minProperties": 1, "additionalProperties": {"type": "string", "minLength": 1}},
     "capabilities": {
       "type": "array",
       "items": {
         "type": "object",
-        "required": ["id", "title", "description", "status"],
+        "required": [
+          "id",
+          "title",
+          "description",
+          "status"
+        ],
         "additionalProperties": false,
         "properties": {
-          "id": {"type": "string", "pattern": "^[a-z0-9_]+(\\.[a-z0-9_]+)+$"},
-          "title": {"type": "string", "minLength": 1},
-          "description": {"type": "string", "minLength": 1},
-          "status": {"$ref": "#/definitions/per_project"}
+          "id": {
+            "type": "string",
+            "pattern": "^[a-z0-9_]+(\\.[a-z0-9_]+)+$"
+          },
+          "title": {
+            "type": "string",
+            "minLength": 1
+          },
+          "description": {
+            "type": "string",
+            "minLength": 1
+          },
+          "status": {
+            "$ref": "#/definitions/per_project"
+          }
         }
       }
     },
@@ -78,14 +215,90 @@
       "type": "array",
       "items": {
         "type": "object",
-        "required": ["code", "behaviour", "emits_event"],
+        "required": [
+          "code",
+          "behaviour",
+          "emits_event"
+        ],
         "additionalProperties": false,
         "properties": {
-          "code": {"type": "string", "pattern": "^(SEY|MOM|MYC|BTC|FLEET)\\.[A-Z][A-Z0-9_]*$"},
-          "behaviour": {"$ref": "#/definitions/per_project", "description": "Does the owning strategy do what the catalog entry's auto_action says?"},
-          "emits_event": {"$ref": "#/definitions/per_project", "description": "Does it write the condition to the shared fleet event log (the only way it reaches a plain-language notification)?"}
+          "code": {
+            "type": "string",
+            "pattern": "^(SEY|MOM|MYC|BTC|FLEET)\\.[A-Z][A-Z0-9_]*$"
+          },
+          "behaviour": {
+            "$ref": "#/definitions/per_project",
+            "description": "Does the owning strategy do what the catalog entry's auto_action says?"
+          },
+          "emits_event": {
+            "$ref": "#/definitions/per_project",
+            "description": "Does it write the condition to the shared fleet event log (the only way it reaches a plain-language notification)?"
+          }
         }
       }
+    },
+    "recent_changes": {
+      "type": "array",
+      "description": "Cells that flipped to done or n/a in the most recent registry edit. Replaced wholesale each time the registry changes -- never appended to -- so the highlighted set on the human page always reflects only the latest edit, giving a visual 'did my update actually land' signal. Omit or set to [] when nothing recent remains.",
+      "items": {
+        "type": "object",
+        "required": [
+          "kind",
+          "id",
+          "project",
+          "note"
+        ],
+        "additionalProperties": false,
+        "properties": {
+          "kind": {
+            "enum": [
+              "cap",
+              "rule"
+            ]
+          },
+          "id": {
+            "type": "string",
+            "minLength": 1,
+            "description": "capability id (kind=cap) or catalog code (kind=rule)."
+          },
+          "project": {
+            "enum": [
+              "momentum",
+              "seykota",
+              "mycrypto",
+              "btc-competition"
+            ]
+          },
+          "aspect": {
+            "enum": [
+              "behaviour",
+              "emits_event"
+            ],
+            "description": "kind=rule only: which cell of that row changed."
+          },
+          "note": {
+            "type": "string",
+            "minLength": 1,
+            "description": "One short line shown in the 'recently changed' summary."
+          }
+        },
+        "allOf": [
+          {
+            "if": {
+              "properties": {
+                "kind": {
+                  "const": "rule"
+                }
+              }
+            },
+            "then": {
+              "required": [
+                "aspect"
+              ]
+            }
+          }
+        ]
+      }
     }
   }
 }
diff --git a/scripts/render_guides.py b/scripts/render_guides.py
index 2828a7c..5ca5647 100644
--- a/scripts/render_guides.py
+++ b/scripts/render_guides.py
@@ -51,6 +51,7 @@ _CSS = """
     --done:#357357; --done-bg:#DEEAE3;
     --pending:#8C6317; --pending-bg:#F1E6C9;
     --na:#6B7566; --na-bg:#E7EAE0;
+    --recent:#A6800A; --recent-bg:#FFF3B0;
   }
   @media (prefers-color-scheme: dark){
     :root:not([data-theme="light"]){
@@ -60,6 +61,7 @@ _CSS = """
       --R3:#E58A7C; --R3-bg:#3A241F; --R2:#D9B364; --R2-bg:#382D18;
       --R1:#7FC6A2; --R1-bg:#1D3227; --R0:#A6AD97; --R0-bg:#292B21;
       --done:#7FC6A2; --done-bg:#1D3227; --pending:#D9B364; --pending-bg:#382D18; --na:#A6AD97; --na-bg:#292B21;
+      --recent:#FFE066; --recent-bg:#4A3B05;
     }
   }
   :root[data-theme="dark"]{
@@ -69,6 +71,7 @@ _CSS = """
     --R3:#E58A7C; --R3-bg:#3A241F; --R2:#D9B364; --R2-bg:#382D18;
     --R1:#7FC6A2; --R1-bg:#1D3227; --R0:#A6AD97; --R0-bg:#292B21;
     --done:#7FC6A2; --done-bg:#1D3227; --pending:#D9B364; --pending-bg:#382D18; --na:#A6AD97; --na-bg:#292B21;
+    --recent:#FFE066; --recent-bg:#4A3B05;
   }
   *{box-sizing:border-box;}
   body{margin:0;background:var(--paper);color:var(--ink);font-family:"Noto Sans TC","IBM Plex Mono",sans-serif;padding:40px 20px 64px;}
@@ -227,8 +230,17 @@ _ROLLOUT_CSS = """
   .dot{display:inline-block;width:9px;height:9px;border-radius:50%;background:var(--dc);border:1px solid var(--dc);}
   .dot-done{--dc:var(--done);} .dot-pending{--dc:var(--pending);} .dot-na{--dc:var(--na);opacity:.55;}
   .dot-none{--dc:var(--line);background:transparent;}
+  .dot-recent{--dc:var(--recent);box-shadow:0 0 0 2px var(--recent-bg);}
   .dot-legend{display:flex;flex-wrap:wrap;gap:4px 14px;font-size:12px;color:var(--ink-faint);margin:0 0 4px;}
   .dot-legend span{display:inline-flex;align-items:center;gap:5px;}
+  .new-badge{display:inline-block;font-size:10px;font-weight:800;letter-spacing:.03em;color:var(--recent);background:var(--recent-bg);border-radius:999px;padding:1px 7px;margin-inline-start:4px;white-space:nowrap;vertical-align:middle;}
+  .recent-box{border:1px solid var(--recent);border-radius:3px;background:var(--recent-bg);padding:12px 14px;margin-bottom:14px;}
+  .recent-box h3{margin:0 0 8px;font-size:13px;color:var(--recent);}
+  .recent-box ul{list-style:none;margin:0;padding:0;display:flex;flex-direction:column;gap:8px;}
+  .recent-box li a{color:inherit;text-decoration:none;font-weight:700;font-size:13.5px;}
+  .recent-box li a:hover,.recent-box li a:focus-visible{text-decoration:underline;}
+  .recent-box .proj{font-weight:600;font-size:11.5px;color:var(--ink-soft);margin-inline-start:6px;}
+  .recent-box .note{display:block;font-weight:400;font-size:12.5px;color:var(--ink-soft);margin-top:2px;}
   .group-head{display:flex;align-items:baseline;gap:10px;margin:40px 0 4px;padding-bottom:8px;border-block-end:2px solid var(--c);}
   .group-head h2{margin:0;color:var(--c);}
   .group-head .n{font-family:"IBM Plex Mono","Noto Sans TC",monospace;font-size:13px;color:var(--ink-faint);}
@@ -265,13 +277,14 @@ _ROLLOUT_CSS = """
 """
 
 _ROLLOUT_JS = _JS_HELPERS + r"""
-var DOT_LABEL={"done":"已做","pending":"未做","na":"不適用","none":"不相關"};
+var DOT_LABEL={"done":"已做","pending":"未做","na":"不適用","none":"不相關","recent":"剛完成"};
 var KINDS=[["cap","機隊功能"],["rule","錯誤處理規則"]];
 function dots(item){
   var s=el("span","dots"); s.setAttribute("aria-hidden","true");
   DATA.project_order.forEach(function(p){ var d=el("span","dot dot-"+item.dots[p]); d.title=DATA.projects[p]+"："+DOT_LABEL[item.dots[p]]; s.appendChild(d); });
   return s;
 }
+function newBadge(){ var b=el("span","new-badge","🆕 最新"); b.title="這次更新剛變成已做／不適用"; return b; }
 function table(item){
   var g=el("div","sl");
   item.rows.forEach(function(r){
@@ -293,13 +306,16 @@ function table(item){
 function pendingCard(item){
   var c=el("div","card item"); c.id=item.anchor;
   var head=el("div","item-head"), h=el("div","h");
-  h.appendChild(el("h3",null,item.title)); h.appendChild(el("p",item.kind==="cap"?"desc":"code",item.sub));
+  h.appendChild(el("h3",null,item.title)); if(item.has_recent) h.appendChild(newBadge());
+  h.appendChild(el("p",item.kind==="cap"?"desc":"code",item.sub));
   head.appendChild(h); head.appendChild(dots(item)); c.appendChild(head);
   c.appendChild(table(item)); return c;
 }
 function doneCard(item){
   var d=el("details","card item done-card"); d.id=item.anchor;
-  var s=el("summary"); s.appendChild(el("span","ok","✓")); s.appendChild(el("span","t",item.title)); s.appendChild(dots(item));
+  var s=el("summary"); s.appendChild(el("span","ok","✓")); s.appendChild(el("span","t",item.title));
+  if(item.has_recent) s.appendChild(newBadge());
+  s.appendChild(dots(item));
   d.appendChild(s);
   var body=el("div","body"); body.appendChild(el("p",item.kind==="cap"?"desc":"code",item.sub)); body.appendChild(table(item));
   d.appendChild(body); return d;
@@ -321,7 +337,8 @@ function tocColumn(group, title){
     var ul=el("ul");
     list.forEach(function(i){
       var li=el("li"), a=el("a"); a.href="#"+i.anchor;
-      a.appendChild(el("span","t",i.title)); a.appendChild(dots(i));
+      a.appendChild(el("span","t",i.title)); if(i.has_recent) a.appendChild(newBadge());
+      a.appendChild(dots(i));
       a.addEventListener("click",function(ev){ ev.preventDefault(); go(i.anchor); });
       li.appendChild(a); ul.appendChild(li);
     });
@@ -336,6 +353,18 @@ function fillGroup(host, group){
     list.forEach(function(i){ host.appendChild(group==="done"?doneCard(i):pendingCard(i)); });
   });
 }
+var recentBox=document.getElementById("recentBox"), recentList=document.getElementById("recentList");
+if(DATA.recent_summary && DATA.recent_summary.length){
+  DATA.recent_summary.forEach(function(r){
+    var li=el("li"), a=el("a"); a.href="#"+r.anchor;
+    a.appendChild(document.createTextNode(r.title));
+    a.appendChild(el("span","proj",r.project));
+    a.appendChild(el("span","note",r.note));
+    a.addEventListener("click",function(ev){ ev.preventDefault(); go(r.anchor); });
+    li.appendChild(a); recentList.appendChild(li);
+  });
+  recentBox.hidden=false;
+}
 var toc=document.getElementById("toc");
 toc.appendChild(tocColumn("pending","未完成")); toc.appendChild(tocColumn("done","已完成"));
 fillGroup(document.getElementById("pending"),"pending");
@@ -441,7 +470,10 @@ def _dot(states: set[str]) -> str:
 
 def rollout_items(catalog: dict[str, Any], registry: dict[str, Any]) -> list[dict[str, Any]]:
     """Every capability and catalog row as one page item.  ``complete`` means no
-    cell is pending -- every applicable strategy is done or n/a, nothing left to do."""
+    cell is pending -- every applicable strategy is done or n/a, nothing left to do.
+    ``recent_changes`` (see the registry schema) overlays a "recent" dot on top of
+    an otherwise done/n-a cell for exactly the (kind, id, project) triples it lists;
+    it never affects ``complete``, which always reflects the real pending/done/n-a state."""
     projects, phases = registry["projects"], registry["phases"]
     titles = {entry["code"]: (entry["risk_tier"], entry["title"]) for entry in catalog["entries"]}
     items = []
@@ -449,7 +481,7 @@ def rollout_items(catalog: dict[str, Any], registry: dict[str, Any]) -> list[dic
         rows = [{"label": projects[p], "lines": [_line(capability["status"][p], None, phases)]}
                 for p in STRATEGY_PROJECTS if p in capability["status"]]
         states = {p: {capability["status"][p]["state"]} if p in capability["status"] else set() for p in STRATEGY_PROJECTS}
-        items.append({"kind": "cap", "anchor": f"cap-{capability['id']}", "title": capability["title"],
+        items.append({"kind": "cap", "id": capability["id"], "anchor": f"cap-{capability['id']}", "title": capability["title"],
                       "sub": capability["description"], "rows": rows, "states": states})
     for row in registry["catalog"]:
         tier, title = titles[row["code"]]
@@ -458,12 +490,17 @@ def rollout_items(catalog: dict[str, Any], registry: dict[str, Any]) -> list[dic
                                                  _line(row["emits_event"][p], "白話通知", phases)]} for p in present]
         states = {p: ({row["behaviour"][p]["state"], row["emits_event"][p]["state"]} if p in present else set())
                   for p in STRATEGY_PROJECTS}
-        items.append({"kind": "rule", "anchor": f"rule-{row['code']}", "title": f"{tier}｜{title}",
+        items.append({"kind": "rule", "id": row["code"], "anchor": f"rule-{row['code']}", "title": f"{tier}｜{title}",
                       "sub": row["code"], "rows": rows, "states": states})
+    recent_set = {(c["kind"], c["id"], c["project"]) for c in registry.get("recent_changes") or []}
     for item in items:
         states = item.pop("states")
         item["dots"] = {p: _dot(s) for p, s in states.items()}
         item["complete"] = "pending" not in set().union(*states.values())
+        for p in STRATEGY_PROJECTS:
+            if item["dots"][p] in ("done", "na") and (item["kind"], item["id"], p) in recent_set:
+                item["dots"][p] = "recent"
+        item["has_recent"] = any(v == "recent" for v in item["dots"].values())
     return items
 
 
@@ -481,9 +518,18 @@ def render_rollout_register(catalog: dict[str, Any], registry: dict[str, Any]) -
     items = rollout_items(catalog, registry)
     n_done = sum(1 for i in items if i["complete"])
     n_pending = len(items) - n_done
+    by_key = {(i["kind"], i["id"]): i for i in items}
+    recent_summary = []
+    for change in registry.get("recent_changes") or []:
+        item = by_key.get((change["kind"], change["id"]))
+        if item is None:
+            continue
+        recent_summary.append({"anchor": item["anchor"], "title": item["title"],
+                                "project": projects[change["project"]], "note": change["note"]})
     data = {
         "projects": projects, "project_order": list(STRATEGY_PROJECTS), "phases": phases,
         "phase_order": list(phases), "phase_counts": phase_counts, "totals": totals, "items": items,
+        "recent_summary": recent_summary,
     }
     sources = "；".join(
         f"{html.escape(registry['projects'][p])} <code>{html.escape(s['repo'])}@{html.escape(s['branch'])} {html.escape(s['commit'][:7])}</code>"
@@ -507,7 +553,12 @@ def render_rollout_register(catalog: dict[str, Any], registry: dict[str, Any]) -
     <span>每項右邊的四個點依序是 {order}：</span>
     <span><i class="dot dot-done"></i>已做</span><span><i class="dot dot-pending"></i>未做</span>
     <span><i class="dot dot-na"></i>不適用</span><span><i class="dot dot-none"></i>跟這支無關</span>
+    <span><i class="dot dot-recent"></i>🆕 最新完成——這次更新才變成已做／不適用</span>
   </p>
+  <div class="recent-box" id="recentBox" hidden>
+    <h3>🆕 最近變動——確認這次更新真的生效了</h3>
+    <ul id="recentList"></ul>
+  </div>
   <nav class="toc" id="toc" aria-label="目錄"></nav>
 
   <div class="group-head c-pending" id="group-pending"><h2>未完成</h2><span class="n">{n_pending} 項</span></div>
diff --git a/src/trade_alerts/catalog/fleet-rollout-registry.json b/src/trade_alerts/catalog/fleet-rollout-registry.json
index 2c3fcc5..c8466f4 100644
--- a/src/trade_alerts/catalog/fleet-rollout-registry.json
+++ b/src/trade_alerts/catalog/fleet-rollout-registry.json
@@ -951,5 +951,13 @@
         }
       }
     }
+  ],
+  "recent_changes": [
+    {
+      "kind": "cap",
+      "id": "ops_export.writer",
+      "project": "momentum",
+      "note": "今天 F3 資金費修復整支重新部署，順帶把這支也帶上了真倉；scripts/repair_bot.py:278 refresh_ops_export 每輪都在改寫 audit/ops_export.json（目前內容是空的）。"
+    }
   ]
 }
diff --git a/src/trade_alerts/rollout_registry.py b/src/trade_alerts/rollout_registry.py
index 9576a5e..a56d2e8 100644
--- a/src/trade_alerts/rollout_registry.py
+++ b/src/trade_alerts/rollout_registry.py
@@ -149,6 +149,35 @@ def registry_problems(registry: Mapping[str, Any], catalog: Mapping[str, Any]) -
             for project, value in (row.get("behaviour") or {}).items():
                 if isinstance(value, Mapping) and value.get("state") != "pending":
                     problems.append(f"{code} is retired but its behaviour for {project} is not pending replacement")
+
+    cap_by_id = {capability.get("id"): capability for capability in registry.get("capabilities") or []}
+    for i, change in enumerate(registry.get("recent_changes") or []):
+        where = f"recent_changes[{i}]"
+        if not isinstance(change, Mapping):
+            problems.append(f"{where}: not an object")
+            continue
+        kind, cid, project = change.get("kind"), change.get("id"), change.get("project")
+        if kind == "cap":
+            capability = cap_by_id.get(cid)
+            if capability is None:
+                problems.append(f"{where}: capability {cid!r} not found")
+                continue
+            state = ((capability.get("status") or {}).get(project) or {}).get("state")
+        elif kind == "rule":
+            row = rows.get(cid)
+            aspect = change.get("aspect")
+            if row is None:
+                problems.append(f"{where}: catalog code {cid!r} not found")
+                continue
+            if aspect not in ("behaviour", "emits_event"):
+                problems.append(f"{where}: rule entries need aspect behaviour/emits_event, got {aspect!r}")
+                continue
+            state = ((row.get(aspect) or {}).get(project) or {}).get("state")
+        else:
+            problems.append(f"{where}: kind must be cap or rule, got {kind!r}")
+            continue
+        if state not in ("done", "n/a"):
+            problems.append(f"{where}: {cid} / {project} is {state!r}, not done/n-a -- recent_changes must point at what just became true")
     return problems
 
 
diff --git a/tests/test_rollout_registry.py b/tests/test_rollout_registry.py
index 91b627c..7de9a84 100644
--- a/tests/test_rollout_registry.py
+++ b/tests/test_rollout_registry.py
@@ -322,3 +322,46 @@ def test_rollout_page_groups_items_by_whether_any_cell_is_pending(registry, cata
     for row in registry["catalog"]:
         pending = any(s["state"] == "pending" for key in ("behaviour", "emits_event") for s in row[key].values())
         assert items[f"rule-{row['code']}"]["complete"] is not pending
+
+
+def test_recent_change_pointing_at_a_still_pending_cell_is_caught(registry, catalog):
+    def break_it(r):
+        capability = r["capabilities"][0]
+        pending_project = next(p for p, s in capability["status"].items() if s["state"] != "done")
+        capability["status"][pending_project] = {
+            "state": "pending", "phase": next(iter(r["phases"])), "reason": "test",
+        }
+        r["recent_changes"] = [{"kind": "cap", "id": capability["id"], "project": pending_project, "note": "x"}]
+
+    problems = _problems_after(registry, catalog, break_it)
+    assert any("not done/n-a" in p for p in problems)
+
+
+def test_recent_change_pointing_at_an_unknown_id_is_caught(registry, catalog):
+    problems = _problems_after(registry, catalog, lambda r: r.update(recent_changes=[
+        {"kind": "cap", "id": "nonexistent.capability", "project": "momentum", "note": "x"},
+    ]))
+    assert any("not found" in p for p in problems)
+
+
+def test_recent_change_on_a_rule_without_aspect_is_caught(registry, catalog):
+    code = registry["catalog"][0]["code"]
+    problems = _problems_after(registry, catalog, lambda r: r.update(recent_changes=[
+        {"kind": "rule", "id": code, "project": "momentum", "note": "x"},
+    ]))
+    assert any("aspect" in p for p in problems)
+
+
+def test_rollout_page_highlights_recent_changes_and_lists_them(registry, catalog):
+    render_guides = _render_guides()
+    items = {i["anchor"]: i for i in render_guides.rollout_items(catalog, registry)}
+    change = registry.get("recent_changes")
+    assert change, "fixture registry should carry at least one recent_changes entry"
+    for entry in change:
+        anchor_prefix = "cap-" if entry["kind"] == "cap" else "rule-"
+        item = items[anchor_prefix + entry["id"]]
+        assert item["dots"][entry["project"]] == "recent"
+        assert item["has_recent"] is True
+    pages = {path.name: html for path, html in render_guides.rendered_pages().items()}
+    assert "dot-recent" in pages["fleet-rollout-register.html"]
+    assert "recent_summary" in pages["fleet-rollout-register.html"]
