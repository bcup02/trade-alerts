# Perplexity 審閱歸檔 — release: v0.22.0 -- trade_correction ledger events, correct_close_v2, catalog text

- **Repo**：bcup02/trade-alerts
- **PR**：#60，最終 head `233e01909a96196ad607d85d8b61cb80b1becebe`，base `main` @ `852f748`，squash 合併為 `8582f26`，tag `v0.22.0`
- **審閱者**：Perplexity — 一輪 **`PASS`**
- **CI**：pytest = success（run `35973084432`；363 passed，+0）
- **對應待辦**：機隊工程進度 `phase-h4`、`phase-h6`、`phase-h7`

## 內容

版本 0.21.1 → 0.22.0，發佈 #58（trade_correction，h4）與 #56（BTC.ORDER_STATUS_UNKNOWN，h7）；h6：SEY.POSITION_AMBIGUOUS 的白話說明補上
「市價單送出後 60 秒查不到」（ai_prompt 已於 v0.21.1 補上，未動）。交接手冊新增發版紀錄（驗證行待部署後補記）。

## 審閱重點

審閱者以 v0.21.1 tag 與 head 的 blob SHA 逐檔比對，證實自 v0.21.1 起 src/ 的程式變更只有 #58、本 PR 零程式碼變更；
錯誤清單改動只限該條三段白話文字且符合不變式；版本號兩處一致、HTML 與 JSON 逐字一致；發版紀錄未宣稱尚未完成的驗證。

## 隔離聲明

無營運環境接觸。完整差異見同名 `.patch`。
