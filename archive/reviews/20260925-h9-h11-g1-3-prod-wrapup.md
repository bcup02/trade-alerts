# Perplexity 審閱歸檔 — docs: h9/h10/h11/g1-3 live-host wrap-up — registry done cells, catalog sources, retire ORPHAN entry

- **Repo**：bcup02/trade-alerts
- **PR**：#71，首審 head `e74c5e1db2bbe3da4a6cbca0b0e9058001ae8aa6`（BLOCK）→ 複審 head `3c9b6b91c8465c5a00c6c8ed41731ab17527d6d8`（PASS），base `main` @ `84a4835`，squash 合併為 `c69e791`
- **審閱者**：Perplexity — **`BLOCK` → `PASS`**
- **CI**：pytest = success（run `36084336230`；381 passed）
- **對應待辦**：機隊工程進度 `phase-h9`、`phase-h11`、`phase-g1`（g1-3）；部署 2026-09-25 01:22–01:27 UTC：btc-competition operations `2d167aa`、momentum `61ff1e5`、seykota `3e3b6f7`
- **發版**：不發版；錯誤目錄資料變動隨下一版 trade-alerts 一起出去

## 做了什麼

部署後收尾（比照 #52）：registry `sources` 換成三個新 operations commit；競賽 `orders.single_send`／`BTC.ORDER_STATUS_UNKNOWN`、趨勢 `SEY.PROTECTION_MOVE_DEFERRED`、`SEY.UNRECORDED_FILL_CORRECTED`／`_UNRESOLVED` 改 done；
`SEY.PROTECTION_ORPHAN_CANCEL_FAILED` 從 registry 與目錄 entries 移除（留在 retired_codes）；三支策略所有既有引用依新舊 commit 的 diff 重算；
SEY./BTC. 目錄 sources 重指到實際發出代碼的行（多數在舊 commit 就已跑位）；`render_guides.py`：behaviour 已做但 emits_event 未做的規則，recent_changes 以前完全不顯示 🆕，已修並加測試。

## 審閱經過

- **首審 BLOCK（證據不足，非已重現的程式錯誤）**：`operations` 指向新 commit 不足以證明正式機在跑；要求跨 repo 逐行核對（特別是 SEY.TRADE_EXIT 1392/1579、BTC.REBALANCE_PENDING 339/347）、退役殘留與產生檔一致性。
- **補證據（PR 留言 issuecomment-5825450449）**：trading-main 上 `/opt/<repo>/{src,scripts,deploy}` 逐檔 sha256 對 `git archive <sources commit>`，三支 156 檔全同；常駐程序 ActiveEnterTimestamp 晚於部署；趨勢修復機器人部署後兩輪含 unrecorded_fill 輸出。限制照實寫：競賽為每日 oneshot，正式機首次執行 2026-09-26 00:10 UTC；UNRECORDED_FILL 兩分支尚無真實案例。登記冊 86 處與目錄 40 處引用逐行附固定 SHA 連結。
- **核對中找到真錯誤（3c9b6b9）**：`BTC.ORDER_STATUS_UNKNOWN` evidence 把 runner.py 的 `:330`／`:385` 省略寫在 executor.py 路徑之後，讀者會對到 executor.py 無關行。`verify_registry_evidence.py` 原本不檢查省略的 `:line`；改為依前一個具名路徑解析並檢查（多 26 處，0 problems），測試加入舊解析器會失敗的案例。
- **範圍外發現**：趨勢策略操作文件仍寫已移除的 `PROTECTIVE_STOP_ORPHAN` → 純文件 AI-for-column/ed-seykota-systematic-trend-following#78（已合併 564a9d4）。
- **複審 PASS**。非阻擋建議：日後補「裸 `:line` 出現在第一個具名路徑之前」及其他含冒號敘述的測試（目前解析器略過無所屬路徑的裸行號，審閱者認為合理）。

## 教訓

- 登記冊 evidence 的每個行號都寫完整路徑，不要用 `:NNN` 省略寫法跨檔案。
- 「正式機在跑」的證據用 `/opt` 逐檔雜湊對 commit，比「operations 指向哪個 commit」強；oneshot 計時器要另外講清楚首次執行時間。

## 隔離聲明

本 PR 只動錯誤目錄／一致性登記冊 JSON、人讀文件、產生頁面、引用檢查器與測試。蒐證只在 trading-main 做唯讀動作（sha256、systemctl show、journalctl、pip show），未讀 /etc、/var/lib 設定，未連交易所或 Google，未改任何服務或帳本。完整差異見同名 `.patch`（src、scripts、tests、docs/fleet-error-catalog.md；docs/guides 兩頁為 render_guides.py 產物，未收入）。
