# g1-5 登記冊收尾（競賽策略帳本少記成交處理上正式機）

- PR：[bcup02/trade-alerts#77](https://github.com/bcup02/trade-alerts/pull/77)，squash 合併 `db7a6d5`（base `7c653d2`）
- Perplexity：首審 **BLOCK**（head `9e8a343`）→ 修正後複審 **PASS**（head `a218b27`）
- CI：pytest 381 passed

## 內容

競賽策略 g1-5（btc-bull-market-competition #37，operations `9559897`，2026-09-26 01:16 UTC 上正式機）：`reconcile.unrecorded_fill_handling`、`BTC.UNRECORDED_FILL_*`（behaviour／emits_event）、`ops_notify.export_relay_enabled`、`ops_notify.units_axis` 改已做；`ops_export.writer` 維持待辦（策略自己的暫停尚未寫進事件日誌，第 8 關）。sources 改到 `9559897`，btc 行號引用從 `2d167aa` 重新對應。

## 審閱

首審 BLOCK 兩處行號：①接在 `runner.py:237` 後、省略路徑的 `:330` 沒重對（應為 `:339`）；②錯誤目錄帶 repo 前綴的 `reconcile_compare.py` 被重對兩次（213→229→245，應為 `:229`）。都是開發端重對腳本的缺陷。修正後以「舊行內容＝新行內容」逐條比對 25 條引用全對，複審 PASS。

教訓：重對行號後要用程式內容比對驗證；`verify_registry_evidence.py` 只檢查行號存在，抓不到指錯行。

非阻擋、另案：`FLEET.LEDGER_DIVERGED_UNIT_EXIT` 標題仍寫「exit 2」，與四支策略現行「每種判定都 exit 0」不符。
