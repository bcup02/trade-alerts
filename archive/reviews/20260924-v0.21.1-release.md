# Perplexity 審閱歸檔 — release: v0.21.1 — data-only patch (catalog + registry) and seykota LINE delivery

- **Repo**：bcup02/trade-alerts
- **PR**：#54，head `9ce09efa85e3ccee9903de021afb215e4080c8fa`，base `main` @ `ef00394`，squash 合併為 `ba6f22e`，tag `v0.21.1`
- **審閱者**：Perplexity — **`PASS`**（一輪）
- **CI**：pytest = success（run `35949830371`；342 passed）
- **對應待辦**：機隊工程進度 `phase-v2-w6`（分級 v2 最後一格）
- **消費專案**：momentum #98、seykota #67、ops-notify #23 改釘（同批 PASS），已部署開發機與正式機；結果見 docs/consumer-release-runbook.md 的 v0.21.1 紀錄。

## 審閱重點與結論

五項全 PASS。審閱者以 blob SHA 逐檔比對 v0.21.0 tag 與 head 的 src/trade_alerts/：22 個 .py 中 21 個完全相同，唯一變動的
`__init__.py` 只有 `__version__` 一行；catalog/ 只有錯誤目錄與登記冊兩份 JSON 變動（前者為 #49／#50／#52 已審閱內容）。
趨勢策略 `notify.line_first_notice` 改 done 符合 #52 定下的完成條件（受控測試事件 f9c595f9…：sent=1、手機收到、下一輪不重送）。

非阻斷侷限：HTML 與 JSON 一致性為邏輯核對（未重跑 render；CI 與合併後 `render_guides.py --check` 已驗證）；
秘密掃描因未開 GHAS 為人工目視。

## 隔離聲明

只動版本號、登記冊 JSON、render 產生的 HTML、發版手冊；無程式行為變更。完整差異見同名 `.patch`。
